import { styled } from "@mui/material/styles";

export default styled('div')(({theme}) => ({
  fontSize: '14px',
  fontStyle: 'normal',
  fontWeight: '500',
  lineHeight: '160%', 
  marginBottom: theme.spacing(2)
}));